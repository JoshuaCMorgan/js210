// What will the following code log to the console and why? 
// Don't run it until you have tried to answer.

if (true) {
  let myValue = 20;
}

console.log(myValue);

// {} identifies a block.  And let will declared a block scoped
// variable.  Therefore, myValue is accessible only within
// the block where it was declared.
