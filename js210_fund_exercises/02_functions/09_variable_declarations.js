/*
What will the following code log to the console and why? 
Don't run the code until after you have tried to answer.
*/

console.log(a);

var a = 1; // undefined
// JS will declare variables and associate scope for the variables.
// It's not until after loggin that a is initialized with a value.
// Up to that point, since 'a' is declared with var keyword, 
// the variable is given undefined. 